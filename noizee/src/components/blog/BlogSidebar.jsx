import React from 'react';

const BlogSidebar = ({ categories, tags, recentPosts }) => {
    // Sidebar cho trang blog (danh mục, tags, bài viết gần đây)
    return <div>Blog Sidebar (Placeholder)</div>;
};
export default BlogSidebar;