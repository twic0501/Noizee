import React from 'react';

const CommentForm = ({ postId /*, onSubmitComment */ }) => {
    // Form để gửi bình luận
    return <form>Comment Form (Placeholder)</form>;
};
export default CommentForm;