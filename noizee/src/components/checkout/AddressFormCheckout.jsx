import React from 'react';

const AddressFormCheckout = ({ onSaveAddress, initialData }) => {
    // Form nhập/chọn địa chỉ giao hàng
    return <div>Address Form (Placeholder)</div>;
};
export default AddressFormCheckout;