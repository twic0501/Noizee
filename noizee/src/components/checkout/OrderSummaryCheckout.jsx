import React from 'react';

const OrderSummaryCheckout = ({ cartItems, subtotal, shippingCost, total }) => {
    // Tóm tắt đơn hàng (sản phẩm, tổng tiền)
    return <div>Order Summary (Placeholder)</div>;
};
export default OrderSummaryCheckout;